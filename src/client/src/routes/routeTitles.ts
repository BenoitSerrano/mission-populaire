import { ROUTE_KEYS } from './routeKeys';

const ROUTE_TITLES: Record<(typeof ROUTE_KEYS)[number], string> = {
    HOME: 'Accueil',
    MISSIONS: 'Missions',
    MISSION_CREATION: 'Créer une mission',
    MISSION_DETAILS: 'Résumé de mission',
    SIGN_IN: 'Connexion',
};

export { ROUTE_TITLES };
